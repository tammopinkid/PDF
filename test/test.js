import { expect as expect } from 'chai';
import PDF from '../src/module.js';

/*describe('PDF', () => {
  it('should be runing without any problems', () => {
    expect(PDF).to.not.throw();
  });
});
*/
